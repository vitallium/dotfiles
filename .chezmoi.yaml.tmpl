---
# vim:ft=gotmpl.yaml
{{ $email := promptString "email" -}}

color: true
progress: true
format: yaml

edit:
  command: "nvim"

data:
  email: {{ $email }}
  pinentry_bin: /opt/homebrew/bin/pinentry-mac
  git:
    gpg:
      key: EA8703DF57BC7168
      bin: gpg

