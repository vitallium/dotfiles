{{- if (lookPath "git-absorb") -}}
#!/bin/bash

# git-absorb version: {{ output "git-absorb" "--version" | trim }}
git-absorb --gen-completions fish > ~/.config/fish/completions/git-absorb.fish
{{- end -}}
