---
gui:
  showIcons: true
git:
  paging:
    colorArg: always
    pager: delta --paging=never
    useConfig: false
os:
  editor: "nvim"
  editAtLine: "{{editor}} --server $NVIM --remote-tab {{filename}}"
